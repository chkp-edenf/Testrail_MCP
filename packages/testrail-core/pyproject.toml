[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "testrail-core"
version = "2.2.0"
description = "TestRail integration core library — HTTP client, caches, rate limit, retry, schemas. Protocol-agnostic; the MCP wrapper is shipped separately."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Eden Fridman" }]
keywords = ["testrail", "qa", "testing", "client", "library"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Testing",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/testrail_core"]
